# Llama Stack Client (agentes) - pré-lançamento
--pre
llama_stack_client>=0.1.0

# Qiskit (computação quântica + simulador)
qiskit>=1.0
qiskit-aer>=0.14

# Variáveis de ambiente
python-dotenv>=1.0

# Visualização (Bloch sphere)
matplotlib>=3.7

# Quantum ML (kernel matrix)
qiskit-machine-learning>=0.7
